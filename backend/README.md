# 📦 Resume Builder – Database Setup Guide

This project includes a fully automated setup for the `resume_builder` MySQL database, including schema creation and bulk data import using CSV files.

---

## 🚀 Quick Start (Recommended)

Run the following command from the `backend` directory:

```bash
python setup.py